language: python
matrix:
    include:
        - python: 3.9
          dist: bionic
          sudo: true
        - python: 3.9
          dist: bionic
          arch: arm64-graviton2
          sudo: true

before_install:
    - pip install poetry

install:
    - poetry install

script:
    - tox
